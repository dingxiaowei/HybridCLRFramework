using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestPrefab : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("这是一个测试prefab");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
